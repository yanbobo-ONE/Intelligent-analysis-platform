---
description: 开发流程管理规则：识别口语化开发指令、阶段 todo、完成后自动同步 Linear，并在失败时重试确认
alwaysApply: true
---
# 开发流程管理规则
## 配置区域

```yaml
project:
  name: "智能分析系统规划"
  team: "智能分析系统规划"
  repo: "https://github.com/yanbobo-ONE/Intelligent-analysis-platform.git"
```

## 触发条件

当用户提到以下操作时，应用此规则：

- 创建项目
- 创建任务
- 新建任务
- 开始做
- 做 xxx
- 完成了
- 搞定
- 取消任务
- 列出任务
- 代办
- 进度
- 提交代码
- 推送
- 创建分支
- 同步到 Linear
- 以及任何明显的开发流程管理、任务同步、分支管理相关表达

## 目标

将用户的日常口语开发指令，自动解析为可执行的开发流程动作，并按优先级处理：

1. 识别当前是在创建、开始、完成、取消，还是查询任务。
2. 识别任务对应的 markdown 文档条目，优先匹配 `p1-D1`、`p1-D2` 这类阶段任务标识。
3. 同步到 Linear，作为 todo 或任务状态更新。
4. 当用户明确说“完成了”但尚未同步 Linear 时，默认自动补同步，不要遗漏。
5. 根据任务类型推荐分支名。
6. 必要时补充下一步开发建议。

## Markdown 到 Linear 的同步规则

当用户的 markdown 文档中存在类似以下格式时：

- `p1-D1`
- `p1-D2`
- `p2-D1`
- `p3-D4`

将其视为阶段性 todo 条目，并同步到 Linear：

- 若 Linear 中已存在对应任务，则更新其状态、标题或描述。
- 若 Linear 中不存在对应任务，则创建为 todo。
- 任务标题尽量保留原文档中的阶段标识，例如：`P1-D1 登录页初始化`。
- 同一篇文档中多个阶段条目时，按顺序逐条同步。

## Linear 同步规则

### 创建类
当用户说：创建项目、创建任务、新建任务、做 xxx、开始做

- 若对应 Linear 项目不存在，先创建项目或确认目标项目。
- 若对应 todo 不存在，先创建 todo。
- 默认状态设置为 `In Progress` 或 Linear 当前等价状态。

### 完成类
当用户说：完成了、搞定、做完了、好了、已完成

- 先自动检查当前上下文中最近操作对应的 todo 是否已完成。
- 如果能明确匹配到任务，直接将对应任务状态更新为 `Done` 或 Linear 当前等价完成状态。
- 如果用户明确说的是某个阶段条目（例如 `P1-D1`、`P1-D2`），则同步更新该阶段的 Linear todo。
- 若任务已在 Linear 中，但未找到明确对应项，先结合最近上下文匹配最近的 todo。
- 对于“完成了本地代码但未同步 Linear”的情况，默认补做 Linear 同步，不要只停留在口头确认。
- 如果自动同步失败，先重试一次；若仍失败，再向用户说明失败原因并请求确认重新匹配/手动指定任务。
### 取消类
当用户说：取消任务、不要了、放弃、作废

- 将对应任务状态更新为取消/已废弃状态，或在 Linear 中标记为不再进行。
- 若无法安全确认目标任务，先询问用户确认。

### 查询类
当用户说：列出任务、代办、进度

- 返回对应项目中的任务列表、当前状态和建议下一步。
- 不要擅自修改状态，除非用户明确要求。

### 提交流程类
当用户说：提交代码、推送、创建分支

- 在回复中给出建议分支名。
- 若任务明确且已关联 Linear，则同时建议同步其状态到合适阶段。
- 不替用户执行危险操作，除非用户明确要求并且上下文清晰。

### 同步失败重试确认

如果自动同步 Linear 失败：

1. 先基于最近上下文重试一次，尝试用阶段标识或任务标题重新匹配。
2. 如果仍失败，明确告诉用户失败原因，并询问是否要重新匹配任务、手动指定 Linear 任务，或重新创建 todo。
3. 不要静默跳过同步结果。
## 分支推荐规则

按固定命名风格生成分支名：

- 功能开发：`feature/<task-slug>`
- 修复问题：`fix/<task-slug>`
- 重构优化：`refactor/<task-slug>`
- 文档更新：`docs/<task-slug>`
- 阶段任务：`phase/<phase-slug>-<task-slug>`

命名要求：
- 全小写
- 使用短横线 `-` 分隔
- 保持简短但可读
- 尽量与 Linear 任务标题一致

## 执行顺序

1. 理解用户口语化意图。
2. 识别是否需要匹配 markdown 中的 `p1-D1`、`p1-D2` 这类条目。
3. 同步或更新 Linear todo。
4. 推荐分支名。
5. 给出下一步开发建议。

## 输出风格

- 先给结论，再给操作建议。
- 尽量简短明确。
- 如果信息不足，优先追问最关键的一项。
- 如果任务标识明确，直接执行，不要反复确认。

## 示例

用户：
> 开始做登录功能

助手应做：
- 识别为创建/开始类任务
- 找到或创建对应 Linear todo
- 更新为进行中
- 推荐分支：`feature/login`

用户：
> 我这边 p1-D1 和 p1-D2 写好了，同步到 Linear

助手应做：
- 识别 markdown 阶段条目
- 创建或更新 Linear todo
- 建议分支：`phase/p1-login-init`

用户：
> 完成了，把这个任务搞定

助手应做：
- 识别为完成类操作
- 将对应任务更新为完成状态
- 给出下一步建议

## 备注

- 这里的配置区域应作为后续维护入口。
- 当 `project.name`、`project.team`、`project.repo` 更新时，优先使用最新配置。
- 如果项目名称与 Linear 中实际名称不一致，以 Linear 中可识别的项目名为准。
- 对于 `p1-D1` 这类文档阶段标识，优先以文档内容为准，再映射到 Linear todo。
